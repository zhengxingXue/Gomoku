from setuptools import setup

setup(name='Gomoku',
      version='0.0.0',
      install_requires=['gym', 'numpy', 'matplotlib', 'pytest',
                        'stable-baselines3', 'coverage', 'pygame']
)
