from setuptools import setup

setup(name='Gomoku',
      version='0.0.0',
      install_requires=['gym', 'numpy', 'matplotlib',
                        'pytest', 'coverage',
                        'stable-baselines3',
                        'pygame', 'pygame_widgets']
)
